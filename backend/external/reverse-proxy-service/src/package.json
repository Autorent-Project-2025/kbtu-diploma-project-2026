{
  "name": "heroku-reverse-proxy-service",
  "version": "1.0.0",
  "description": "Custom service for proxying requests within the Heroku platform",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run build && npm run start",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.5",
    "typescript": "^5.9.3"
  }
}
