{
  "name": "email-service",
  "version": "1.0.0",
  "description": "SMTP email service for sending notification templates",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "node --env-file-if-exists=../.env --experimental-strip-types index.ts",
    "dev": "node --watch --env-file-if-exists=../.env --experimental-strip-types index.ts"
  },
  "dependencies": {
    "amqplib": "^0.10.9",
    "nodemailer": "^8.0.1"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/nodemailer": "^7.0.11"
  }
}
